@model List<ShoppingCart.ViewModels.BookViewModel>
    @{ 
        ViewBag.Title = "Books";
    }
<h2>图书列表</h2>
@if (Model.Count>0)
{
    @Html.Partial("_List",Model)
}
else
{
    <div class="alert alert-info">
        当前没有图书供显示
    </div>
}
